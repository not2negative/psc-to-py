temperatura_celsius = float(input("Ingrese la temperatura en C°: "))

if (temperatura_celsius >= 15 and temperatura_celsius <= 85):
    print("Rango seguro para la temperatura")
else:
    print("Rango inseguro para la temperatura: cancelar")